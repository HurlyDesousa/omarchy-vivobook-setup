# Omarchy bar plugin stubs shipped by this repo.
#
#   sw.art.autostart/   — login autostart bar widget (needs omarchy-autostart-apps CLI)
#
# Other plugins (task-manager, kbd-backlight) come from omarchy-task-manager install.
# install-all.sh symlinks/copies plugins under ~/.config/omarchy/plugins/
